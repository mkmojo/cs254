cmake_minimum_required (VERSION 2.8.8)
project (Assignment5)

# use library
include_directories ( "${PROJECT_SOURCE_DIR}/lib" )
add_subdirectory (lib)

# add the executable
add_executable(main main.cpp)
target_link_libraries (main Oset) 
